public class Array{
    public static void main(String[]args){
        int arr[]={12,13,66,99};
        int sum=0;
        for(float num :arr){
           sum+=num;
        }
        double avg=sum/arr.length;
        System.out.println(avg);
    }
}
